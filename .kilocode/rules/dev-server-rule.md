# Dev Server Rule

Guidelines for handling the development server.

## Rules

- Do not run the dev server
- Always assume it's running

## Guidelines

If you want to run the dev server, ask the user if they're running it. You by yourself will never run the dev server.
