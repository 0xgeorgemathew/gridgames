'use client'

/**
 * RoundEndFlash - Previously showed round end results.
 * Now returns null since rounds have been removed.
 * Kept as a placeholder to avoid breaking imports.
 */
export function RoundEndFlash() {
  return null
}
